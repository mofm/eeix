# Bash aliases and functions for root user.
# System-wide environments setup in /etc/profile file. - Personel environments setup in ~/.bashrc file.

if [ -f "/etc/bash/bashrc" ] ; then
    source /etc/bash/bashrc
fi
